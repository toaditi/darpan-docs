---
title: "Roadmap"
description: "Improvements and features coming to the Darpan engine and app, in order — plus what shipped most recently."
---

What we shipped most recently and what we are building next, in order. Shipped work moves to [Release updates](/releases/updates); the order below is influenced by [feature requests](/releases/feature-requests).

## Recently shipped

<div className="rm-card">
  <p><strong>v1.2.0 — chat spaces, run control, connection tests</strong> <span className="rm-pill">Shipped · Aug 1, 2026</span></p>
  <p className="rm-muted">Send run notifications to named Google Chat spaces per tenant, watch a run live and stop it mid-run, test a saved Shopify or OMS connection from its dashboard, and set a preferred timezone that drives the whole app. Ships with app v2.3.0; see [Release updates](/releases/updates).</p>
</div>

<div className="rm-card">
  <p><strong>v1.1.0 — composite keys + live run visibility</strong> <span className="rm-pill">Shipped · Jul 27, 2026</span></p>
  <p className="rm-muted">Match records on a combination of key fields, watch runs progress live through a step timeline, and trust missing-in-Shopify results verified by direct lookups. Ships with app v2.2.0; see [Release updates](/releases/updates).</p>
</div>

## Up next, in order

<div className="rm-timeline">
  <div className="rm-step">
    <div className="rm-step-marker">1</div>
    <div>
      <p><strong>Idempotency keys for API mutations</strong> <span className="rm-pill-next">Next up</span> <span className="rm-pill-surface">Engine</span></p>
      <p className="rm-muted">Retry any mutating API call safely without duplicating work.</p>
    </div>
  </div>
  <div className="rm-step">
    <div className="rm-step-marker">2</div>
    <div>
      <p><strong>Returns reconciliation</strong> <span className="rm-pill-surface">Engine · App</span></p>
      <p className="rm-muted">Track returns against source and target systems, the same way orders are reconciled today.</p>
    </div>
  </div>
  <div className="rm-step">
    <div className="rm-step-marker">3</div>
    <div>
      <p><strong>Fulfillments reconciliation</strong> <span className="rm-pill-surface">Engine · App</span></p>
      <p className="rm-muted">Track shipments against source and target systems, the same way orders are reconciled today.</p>
    </div>
  </div>
  <div className="rm-step">
    <div className="rm-step-marker">4</div>
    <div>
      <p><strong>Multi-file-source stitching for a run</strong> <span className="rm-pill-surface">Engine</span></p>
      <p className="rm-muted">Combine multiple files into one side of a run instead of merging them by hand first.</p>
    </div>
  </div>
</div>

<p className="rm-muted">Order and scope reflect current planning and can change. Want to influence this list? Vote on [feature requests](/releases/feature-requests).</p>
